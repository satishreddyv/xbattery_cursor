/**
 * @file system_S32K144.c
 * @brief System initialization for S32K144 - Xbattery
 */

#include <stdint.h>

extern void SystemInit(void);

/**
 * Minimal system init (clock, etc.).
 * Expand with S32K144 clock and peripheral setup as needed.
 */
void SystemInit(void)
{
    /* Placeholder: add clock config, flash wait states, etc. when using SDK. */
    (void)0;
}
